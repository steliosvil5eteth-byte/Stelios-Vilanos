import {requireAdmin} from '../_lib/access.js';import {getIncidentState,setIncidentState} from '../_lib/incident.js';import {requestId} from '../_lib/request-trace.js';export default async function handler(req,res){try{const rid=requestId(req,res);const a=await requireAdmin(req);if(req.method==='GET')return res.status(200).json({requestId:rid,incident:await getIncidentState()});if(req.method==='PUT')return res.status(200).json({ok:true,requestId:rid,incident:await setIncidentState(a.username,req.body||{})});return res.status(405).json({error:'GET or PUT only'})}catch(e){return res.status(e.status||500).json({error:e.message,code:e.code})}}
