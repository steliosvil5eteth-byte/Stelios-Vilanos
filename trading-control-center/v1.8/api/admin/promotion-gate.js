import {requireAdmin} from '../_lib/access.js';import {promotionGate} from '../_lib/promotion.js';import {requestId} from '../_lib/request-trace.js';export default async function handler(req,res){try{const rid=requestId(req,res);await requireAdmin(req);if(req.method!=='GET')return res.status(405).json({error:'GET only'});const gate=await promotionGate({minSamples:Number(req.query?.minSamples)||3,targetAvailability:Number(req.query?.target)||99});return res.status(gate.ready?200:409).json({requestId:rid,gate})}catch(e){return res.status(e.status||500).json({error:e.message,code:e.code})}}
