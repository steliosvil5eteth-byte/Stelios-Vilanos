const {checkDataIntegrity}=await import('../api/_lib/integrity.js');const {getSafetyState}=await import('../api/_lib/safety.js');const {getMaintenanceState}=await import('../api/_lib/maintenance.js');const {getReleaseControl}=await import('../api/_lib/release-control.js');const {migrationStatus}=await import('../api/_lib/migrations.js');const {APP_VERSION}=await import('../api/_lib/release.js');const [integrity,safety,maintenance,release,migrations]=await Promise.all([checkDataIntegrity(),getSafetyState(),getMaintenanceState(),getReleaseControl(),migrationStatus()]);const checks={integrity:integrity.ok,liveExecutionOff:true,paperBlockedUnlessExplicit:!safety.paperExecutionAllowed||(!safety.emergencyStop&&safety.paperExecutionAdminEnabled&&safety.paperExecutionEnvEnabled),releaseControl:Boolean(release),migrationsKnown:Boolean(migrations)};console.log(JSON.stringify({version:APP_VERSION,checks,integrity,safety,maintenance,release,migrations},null,2));if(!Object.values(checks).every(Boolean))process.exitCode=2;
