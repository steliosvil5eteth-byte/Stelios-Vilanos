function finite(v){return Number.isFinite(Number(v))}
export function normalizeSymbols(input,max=25){const arr=Array.isArray(input)?input:String(input||'').split(',');const out=[...new Set(arr.map(x=>String(x).trim().toUpperCase()).filter(Boolean))];if(out.some(x=>!/^[A-Z0-9.\-]{1,15}$/.test(x))){const e=new Error('Invalid symbol format');e.status=400;e.code='INVALID_SYMBOL';throw e}if(out.length>max){const e=new Error(`Too many symbols; max ${max}`);e.status=400;e.code='SYMBOL_LIMIT';throw e}return out}
export function validateLongPaperOrder(o={}){const symbol=String(o.symbol||'').trim().toUpperCase();if(!/^[A-Z0-9.\-]{1,15}$/.test(symbol)){const e=new Error('Invalid order symbol');e.status=400;e.code='INVALID_ORDER';throw e}for(const k of ['qty','entry','stop','target'])if(!finite(o[k])||Number(o[k])<=0){const e=new Error(`Invalid order ${k}`);e.status=400;e.code='INVALID_ORDER';throw e}if(!(Number(o.stop)<Number(o.entry)&&Number(o.entry)<Number(o.target))){const e=new Error('Long order requires stop < entry < target');e.status=400;e.code='INVALID_ORDER';throw e}return {...o,symbol,qty:Number(o.qty),entry:Number(o.entry),stop:Number(o.stop),target:Number(o.target),strategyId:String(o.strategyId||'').slice(0,80)}}
export function validateBracketOrder(o={}){if(!finite(o.conid)||Number(o.conid)<=0){const e=new Error('Invalid conid');e.status=400;e.code='INVALID_ORDER';throw e}const base=validateLongPaperOrder({symbol:o.symbol||'IBKR',qty:o.quantity,entry:o.entry,stop:o.stop,target:o.target,strategyId:o.strategyId});return {...o,conid:Number(o.conid),quantity:base.qty,entry:base.entry,stop:base.stop,target:base.target,strategyId:base.strategyId}}
