function daysBetween(a,b){return Math.abs((b-a)/86400000)}
export function marketFreshness(series,{maxAgeDays=Number(process.env.MARKET_DATA_MAX_AGE_DAYS||7),now=new Date()}={}){const last=Array.isArray(series)?series.at(-1):null;if(!last?.date)return {ok:false,ageDays:null,lastDate:null,reason:'NO_LAST_BAR'};const d=new Date(`${last.date}T23:59:59Z`);if(Number.isNaN(d.getTime()))return {ok:false,ageDays:null,lastDate:last.date,reason:'INVALID_LAST_BAR_DATE'};const age=daysBetween(d,now);return {ok:age<=maxAgeDays,ageDays:age,lastDate:last.date,maxAgeDays,reason:age<=maxAgeDays?'FRESH':`STALE>${maxAgeDays}D`}}
