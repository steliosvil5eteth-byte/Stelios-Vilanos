name: Trading Control Center v1.6 Validate
on:
  push:
    paths:
      - 'trading-control-center/v1.6/**'
      - '.github/workflows/trading-control-center-v1.6-validate.yml'
      - '.github/workflows/trading-control-center-v1.6-deploy.yml'
  pull_request:
    paths:
      - 'trading-control-center/v1.6/**'
      - '.github/workflows/trading-control-center-v1.6-validate.yml'
      - '.github/workflows/trading-control-center-v1.6-deploy.yml'
jobs:
  validate:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: trading-control-center/v1.6
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install --ignore-scripts
      - run: npm run validate
      - run: npm run preflight
