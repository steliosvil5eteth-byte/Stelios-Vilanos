const {sloSnapshot}=await import('../server/api/_lib/slo.js');const {getIncidentState}=await import('../server/api/_lib/incident.js');const {retentionDryRun}=await import('../server/api/_lib/retention.js');const {deploymentManifest}=await import('../server/api/_lib/deployment-manifest.js');const [slo,incident,retention,manifest]=await Promise.all([sloSnapshot({hours:24}),getIncidentState(),retentionDryRun(),deploymentManifest()]);console.log(JSON.stringify({slo,incident,retention,manifest:{version:manifest.version,sha256:manifest.sha256,paperOnly:manifest.paperOnly,liveExecution:manifest.liveExecution}},null,2));if(manifest.liveExecution!==false)process.exitCode=2;
