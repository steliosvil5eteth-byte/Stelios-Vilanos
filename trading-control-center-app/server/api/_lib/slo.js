import {monitoringEvents,systemSnapshot} from './monitoring.js';
export async function sloSnapshot({hours=24,targetAvailability=99.0}={}){const since=Date.now()-Math.max(1,Number(hours)||24)*3600e3,rows=(await monitoringEvents(500)).filter(x=>Date.parse(x.ts||0)>=since),health=rows.filter(x=>x.type==='HEALTH_CHECK'),good=health.filter(x=>x.severity!=='critical').length,total=health.length,availability=total?good/total*100:null,critical=rows.filter(x=>x.severity==='critical').length,system=await systemSnapshot();return {windowHours:hours,targetAvailabilityPct:targetAvailability,samples:total,goodSamples:good,availabilityPct:availability,criticalEvents:critical,status:total<3?'INSUFFICIENT_DATA':availability>=targetAvailability?'MEETING':'BREACH',systemOk:system.ok,databaseLatencyMs:system.database?.latencyMs??null,asOf:new Date().toISOString()}}
